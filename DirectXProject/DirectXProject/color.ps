// HLSL 셰이더
// 화면에 그려지는 도형의 각 픽셀을 그리기 위한 튜토리얼 HLSL 파일
// 픽셀 셰이더
// 정점 셰이더의 결과물을 픽셀 셰이더에서 사용한다


struct PixelInputType
{
	float4 position: SV_POSITION;
	float4 color: COLOR;
};

float4 ColorPixelShader(PixelInputType input) : SV_Target
{
	return input.color;
}